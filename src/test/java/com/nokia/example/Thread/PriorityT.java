package com.nokia.example.Thread;

/**
 * Created by YLongYao on 2016/11/26.
 */
public class PriorityT {
    /**
     * 线程优先级：
     *      java 中的线程优先级的范围是1～10，默认的优先级是5。“高优先级线程”会优先于“低优先级线程”执行。
     *      java 中有两种线程：用户线程和守护线程。可以通过isDaemon()方法来区别它们：如果返回false，
     *          则说明该线程是“用户线程”；否则就是“守护线程”。
     *      用户线程一般用户执行用户级任务，而守护线程也就是“后台线程”，一般用来执行后台任务。
     *          需要注意的是：Java虚拟机在“用户线程”都结束后会后退出。
     */
}
